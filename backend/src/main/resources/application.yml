server:
  port: 8080

spring:
  application:
    name: competition-platform

  datasource:
    url: jdbc:mysql://localhost:3306/competition_platform?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Cqs9768685473
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

  sql:
    init:
      mode: always
      schema-locations: classpath:sql/schema_v3.sql
      data-locations: classpath:sql/init.sql
      encoding: UTF-8

jwt:
  secret: competitionPlatformSecretKey2023
  expiration: 86400 # 24 hours

logging:
  level:
    com.competition: INFO
